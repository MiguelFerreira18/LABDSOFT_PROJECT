package isep.ipp.pt.Smart_cities.Respository;
/* 
import isep.ipp.pt.Smart_cities.Model.UserModel.InstitutionApplication;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface InstitutionApplicationRepository extends JpaRepository<InstitutionApplication, String> {
    // Define custom query methods if needed

    List<InstitutionApplication> findByStatus(String status);
    List<InstitutionApplication> findByUser_Id(String userId);
   List<InstitutionApplication> findByUserId(String userId);

}
   */